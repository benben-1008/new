# 予約システムの仕組み

## データの流れ

### 1. 予約の保存（book.html → data/sales-data.json）

```
book.html
  ↓ (ユーザーが予約フォームを送信)
book.html内のJavaScript (submitReservation関数)
  ↓ (1) 既存の予約を取得
  GET api/sales-data.php?reservations=true
  ↓ (2) 新しい予約を配列に追加
  ↓ (3) 配列全体を保存
  POST api/sales-data.php (action: 'save-all')
  ↓
api/sales-data.php
  ↓ (saveAllReservations関数で保存)
data/sales-data.json
```

### 2. 予約の読み込み（admin.html ← data/sales-data.json）

```
admin.html
  ↓ (loadReservations関数)
  GET api/sales-data.php?reservations=true
  ↓
api/sales-data.php
  ↓ (getAllReservations関数で読み込み)
data/sales-data.json
  ↓
admin.html (予約一覧を表示)
```

## ファイル構成

- **book.html**: 予約フォーム（ユーザーが予約するページ）
- **api/sales-data.php**: 予約データと売上データの保存・読み込みAPI
- **data/sales-data.json**: 予約データと売上データの保存先（JSONファイル）
- **admin.html**: 管理画面（予約一覧を表示）

## データ形式（sales-data.json）

```json
{
  "2026-02-18": {
    "reservations": 5,
    "people": 3,
    "menuSales": {
      "カレーライス": 2,
      "うどん": 1
    },
    "reservationList": [
      {
        "id": 1234567890,
        "date": "2026-02-18",
        "time": "12:30",
        "name": "田中太郎",
        "studentClass": "",
        "people": 1,
        "food": "カレーライス",
        "reservationNumber": 123,
        "verified": false
      }
    ]
  }
}
```

## 確認方法

### 1. 予約が保存されているか確認
- InfinityFreeのファイルマネージャーで `data/sales-data.json` を開く
- JSON形式で予約データが`reservationList`に保存されているか確認

### 2. ブラウザのコンソールで確認
- `book.html` で予約送信時: `[予約デバッグ]` のログを確認
- `admin.html` で予約読み込み時: `[管理画面デバッグ]` のログを確認

### 3. APIを直接確認
- ブラウザで `https://あなたのドメイン.infinityfreeapp.com/api/sales-data.php?reservations=true` にアクセス
- JSON配列が返ってくるか確認

## トラブルシューティング

### 予約が保存されない場合
1. `data/` フォルダが存在するか確認
2. `data/sales-data.json` の書き込み権限を確認（755または777）
3. ブラウザのコンソール（F12）でエラーを確認

### admin.htmlで予約が表示されない場合
1. `data/sales-data.json` にデータが保存されているか確認（`reservationList`内）
2. フィルター設定を確認（「表示」を「すべて」にする）
3. ブラウザのコンソール（F12）でエラーを確認

## 変更履歴

- 2026-02-18: `reservations.json`から`sales-data.json`に統合
- 2026-02-18: `reservation.html`を`book.html`に置き換え
